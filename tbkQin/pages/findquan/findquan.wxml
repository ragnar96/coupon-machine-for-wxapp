


<!--des-->
<view wx:if = "{{ifsp}}">
<view style='text-align:center; margin:15rpx; font-weight:600'>
<text>
会议入场券设计展示
</text>
</view>

  <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
        <block wx:for="{{designimgUrls}}">
            <swiper-item>
                <image src="{{item}}" class="slide-image"/>
            </swiper-item>
        </block>
    </swiper>

<view style='text-align:center; margin:15rpx; font-weight:600'>
<text>
宴席入场券设计展示
</text>
</view>


      <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
        <block wx:for="{{designimgUrls}}">
            <swiper-item>
                <image src="{{item}}" class="slide-image"/>
            </swiper-item>
        </block>
    </swiper>

<view style='text-align:center; margin:15rpx; font-weight:600'>
<text>
婚礼入场券设计展示
</text>
</view>


      <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
        <block wx:for="{{designimgUrls}}">
            <swiper-item>
                <image src="{{item}}" class="slide-image"/>
            </swiper-item>
        </block>
    </swiper>

<view style='text-align:center; margin:15rpx;font-weight:600'>
<text>
讲座入场券设计展示
</text>
</view>


      <swiper indicator-dots="{{indicatorDots}}" autoplay="{{autoplay}}" interval="{{interval}}" duration="{{duration}}">
        <block wx:for="{{designimgUrls}}">
            <swiper-item>
                <image src="{{item}}" class="slide-image"/>
            </swiper-item>
        </block>
    </swiper>

</view>

<!--des-->





<view wx:if = "{{!ifsp}}">

<view class="container_detail">

  <view class="searchbeforeimage">
   
      <image class="searchheader" src="{{speechword.imglo}}" background-size="cover"></image>
      <text class="searchmotto">{{speechword.souquanba}}</text>
  </view>

<!--搜索栏-->

   <view class="search-wrapper"> 
      <view class="search-section">
        <view class="search-input-wrapper " >
        
         <image class="search-image" src="../../resources/images/search.png"  style="float:left;margin-right:20rpx;"></image>

         <image class="search-image" src="../../resources/images/cancelicon.png" bindtap="clearsearchtext" style="float:right;margin-left:20rpx;"></image>

          <input class="search-input" placeholder="{{speechword.souquanbrief}}" value="{{searchtext}}" bindinput="getsearchtext" confirm-type="search"  />   

        </view>
        <view class="search-button-wrapper">
          <button class="search-button" bindtap="searchclick">搜索</button>
        </view>
      </view>
  </view>

<!--搜索栏--> 


<view class='undertips'>
  <div class='hotips' bindtap="leadtipfir">
    男装
  </div>
  <div class='hotips' bindtap="leadtipsec">
    女装
  </div>
  <div class='hotips' bindtap="leadtipthr">
    内衣
  </div>
  <div class='hotips' bindtap="leadtipfor">
    运动
  </div>
  <div class='hotips' bindtap="leadtipfif">
    童装
  </div>
  <div class='hotips' bindtap="leadtipsix">
    家居
  </div>
  
</view>




 </view>
      
</view>
